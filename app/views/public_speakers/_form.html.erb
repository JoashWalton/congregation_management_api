<%= form_with(model: public_speaker, local: true) do |form| %>
  <% if public_speaker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(public_speaker.errors.count, "error") %> prohibited this public_speaker from being saved:</h2>

      <ul>
        <% public_speaker.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
