<%= form_with(model: field_service_group, local: true) do |form| %>
  <% if field_service_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(field_service_group.errors.count, "error") %> prohibited this field_service_group from being saved:</h2>

      <ul>
        <% field_service_group.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_field :location %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
